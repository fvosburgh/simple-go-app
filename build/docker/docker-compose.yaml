version: "3.4"

services:
  app:
    build:
      context: ../../../
      dockerfile: ./build/docker/sdk/api/Dockerfile
    image: demo/sample-app:${GIT_COMMIT_SHORT_HASH:-docker}
    container_name: sample-app
    environment:
      - SAMPLE_ENV=demo
    ports:
      - "8080:8080"
